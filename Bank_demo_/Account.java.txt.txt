**Class: Account**

**Description:**
Represents an account in the system.

**Attributes:**

* **id:** Unique identifier for the account.
* **name:** Name of the account holder.
* **email:** Email address of the account holder.
* **phoneNumber:** Phone number of the account holder.
* **status:** Status of the account (e.g., approved, rejected, pending).

**Getters and Setters:**
Standard getters and setters are provided for all attributes.

**Example Usage:**

```java
// Create a new account
Account account = new Account();
account.setName("John Doe");
account.setEmail("johndoe@example.com");
account.setPhoneNumber("123-456-7890");
account.setStatus("approved");

// Get the account's name
String name = account.getName();

// Set the account's email address
account.setEmail("new_email@example.com");
```

**Additional Notes:**

* The `@Id` annotation indicates that the `id` field is the primary key of the entity.
* The `@GeneratedValue(strategy = GenerationType.IDENTITY)` annotation specifies that the `id` field should be auto-generated by the database using the IDENTITY strategy. This means that the database will automatically generate a unique ID for each new account.